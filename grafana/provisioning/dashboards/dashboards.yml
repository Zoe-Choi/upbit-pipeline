apiVersion: 1

providers:
  - name: 'Upbit Pipeline Dashboards'
    orgId: 1
    folder: 'Upbit'
    folderUid: 'upbit'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
